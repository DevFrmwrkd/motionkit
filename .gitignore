node_modules/
app/node_modules/
.next/
app/.next/
.turbo/
dist/
out/
app/out/
app/.open-next/
app/.vercel/
app/.wrangler/

# Built sandbox bundle (produced by app/scripts/build-sandbox.mjs)
app/public/sandbox/bundle.js
*.tsbuildinfo

# Environment
.env
.env.local
.env*.local

# Convex
# Note: convex/_generated/ is committed so Vercel (and any build
# environment without CONVEX_DEPLOY_KEY) can typecheck + build without
# running `convex codegen`. The Convex CLI guards against running
# codegen inside Vercel's build env — committing the output is the
# standard workaround. Regenerate locally with `npx convex codegen`
# after any schema.ts change.

# Renders & media
*.mp4
*.webm
*.mov

# OS
.DS_Store
Thumbs.db
._*

# IDE
.vscode/
.idea/

# Temp / seed data (not part of the build)
temp/
tmp/

# Debug
npm-debug.log*
pnpm-debug.log*
.vercel/

/.agents
/.claude
/.kiro
/.windsurfer
skills

AGENTS.md
CLAUDE.md
.vercel

# Memory compiler runtime state (daily logs, compiled knowledge, caches)
.memory-compiler/.venv/
.memory-compiler/daily/
.memory-compiler/knowledge/
.memory-compiler/reports/
.memory-compiler/scripts/state.json
.memory-compiler/scripts/last-flush.json
.memory-compiler/scripts/flush.log
.memory-compiler/scripts/compile.log
.memory-compiler/scripts/session-flush-*
.memory-compiler/scripts/flush-context-*
.memory-compiler/__pycache__/
.memory-compiler/**/__pycache__/
